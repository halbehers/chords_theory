#pragma once

namespace component
{

// Shared across every Synth tab module section (ADSR/LFO/Filter) so the spacing between each
// section's border and its content stays consistent and is only ever tuned in one place.
constexpr float kSynthSectionPadding = 8.0f;

}
